import torch

def Network(net_name, need_layer=None, is_det=False):

    return _get_detection_network(net_name)

#TODO: get the basic_network and rcnn network, respectively
def _get_detection_network(net_name):

    pass
